# Set the SSH agent socket for 1Password across the entire GNOME session.
# This file is read by systemd --user, so SSH_AUTH_SOCK is available to all
# graphical apps (not just terminals), without needing a .desktop autostart entry.
SSH_AUTH_SOCK="${HOME}/.1password/agent.sock"
