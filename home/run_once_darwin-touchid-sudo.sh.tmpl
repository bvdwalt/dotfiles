{{ if eq .chezmoi.os "darwin" -}}
#!/bin/bash

# Enable Touch ID for sudo (sudo_local survives OS updates)
if [ ! -f /etc/pam.d/sudo_local ]; then
  echo "auth       sufficient     pam_tid.so" | sudo tee /etc/pam.d/sudo_local
fi
{{ end -}}
